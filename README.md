# codevigil

Local, privacy-preserving observability for Claude Code sessions. codevigil tails session JSONL on disk, computes signal metrics about reasoning and tool-use patterns, and surfaces them in a terminal dashboard or as JSON reports — never leaving the host, with no network egress and a stdlib-only runtime.
